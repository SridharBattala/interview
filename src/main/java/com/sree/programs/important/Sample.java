package com.sree.leave.programs.important;
/*
 * https://www.geeksforgeeks.org/sort-elements-by-frequency/
 */
public class Sample {
	/*
	 * Given a list of strings, return a list of strings ordered by first
	 * occurrences (primary) and then recency (secondary).
	 * 
	 * Example input: b, a, c, a, b, b, a, c output: a -> b -> c a occurred 3 times,
	 * b occurred 3 times, c occurred 2 times. a and b has higher occurrences than
	 * c, so they are ordered before c. a has higher recency than b, so a is ordered
	 * before b.
	 */
	
	
}
